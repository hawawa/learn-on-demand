version: 2

sources:
  - name: snowplow
    database: raw
    schema: snowplow
    tables:
      - name: events
        config:
          loaded_at_field: collector_tstamp
          freshness:
            warn_after: {count: 6, period: hour}
            error_after: {count: 24, period: hour}